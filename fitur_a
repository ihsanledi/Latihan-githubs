hallo semuanya

3 area git 
WD
SD


cek file udah masuk ke staging area(SD) atau belum pake git status

lakuin commit bisa dengan ketik git commit dan kasih pesan pada filenya

cara cek email udah ke setting blm pake

git config --global -l

kalo mau ubah pake 

git config --global user.name "Nama user"
git config --global user.email "Nama email"

cara melihat jejak commit bisa peke
git log

kalo mau ngubah head commit ke versi tertentu copy commit hash di git log dan ketik

git checkout /commit hash

cara bikin new branch bisa pake switch atau checkout

kalo kita mau balik ke versi sebelumnya terus mau ngambil versi itu buat jadi head kita ubah dulu head dari versinya dengan
git checkout commit hashnya

terus bikin branch baru dengan switch branch
git switch -c nama_branch

terus kalo udah di switch dan mau dimerge
balik ke branch main/ branch utama

terus lakuin merge dengan git merge nama_branch_bar